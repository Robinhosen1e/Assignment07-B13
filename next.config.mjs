/** @type {import('next').NextConfig} */
const nextConfig = {
  reactCompiler: true,

  images: {
    domains: ['i.pravatar.cc', 'images.unsplash.com' , 'i.ibb.co.com'],
  },
};

export default nextConfig;